import { BackgroundBox, ImageBox } from './PopularBackground.styled';
const PopularBackground = () => {
  return (
    <BackgroundBox>
      <ImageBox />
    </BackgroundBox>
  );
};

export default PopularBackground;
